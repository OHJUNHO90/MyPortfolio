SELECT MES_PROD_SEQ , COUNT(*)  
FROM (   
	SELECT *
	FROM(
		SELECT  A.MES_PROD_SEQ,
			A.BODY_NO,
			DP_CMT
		FROM TB_IF_MES_PROD A 
		ORDER BY A.MES_PROD_SEQ DESC)
		WHERE ROWNUM <300
	)			   
GROUP BY MES_PROD_SEQ
HAVING  COUNT(*)  > 1
;